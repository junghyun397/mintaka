[package]
name = "rusty_renju"
version = "0.9.0"
edition = "2024"

[features]
default = ["strict_renju"]
strict_renju = []

[lib]
name = "rusty_renju"
crate-type = ["rlib", "cdylib"]

[dependencies]
ethnum = "1.5.0"
regex-lite = "0.1.6"
rand = "0.9.0-alpha.2"
smallvec = "1.13.2"

[dev-dependencies]
indoc = "2.0.5"
