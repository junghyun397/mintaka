#![feature(test)]

extern crate test;
use test::Bencher;

#[bench]
fn possible_position(b: &mut Bencher) {
    b.iter(|| {

    })
}

#[bench]
fn deep_nested_position(b: &mut Bencher) {
    b.iter(|| {

    })
}
