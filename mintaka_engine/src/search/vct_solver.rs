use mintaka::game::Game;
use mintaka::notation::pos::Pos;

// Proof-Number Search(PNS), https://www.chessprogramming.org/Proof-Number_Search
pub fn find_vct_solution(game: &Game, max_depth: usize) -> Option<Pos> {
    todo!()
}
