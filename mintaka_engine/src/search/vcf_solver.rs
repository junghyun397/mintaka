use mintaka::game::Game;
use mintaka::notation::pos::Pos;

// Iterative Deepening Search(IDS), https://www.chessprogramming.org/Iterative_Deepening
pub fn find_vcf_solution(board: &Game, max_depth: usize) -> Option<Pos> {
    todo!()
}
